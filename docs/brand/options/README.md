# Flowmux Logo Options

`logo-mark-options.svg` contains ten editable vector concepts. Each card pairs
a standalone mark with a matching wordmark lockup.

The current committed logo in `docs/brand/` is unchanged. Once a direction is
chosen, split that option into final `flowmux-mark.svg` and `flowmux-logo.svg`
assets.
