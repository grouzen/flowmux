# Flowmux Switchyard Sketches

`switchyard-sketches.svg` contains twenty black-only mark sketches based on
the reference-board direction.

Use this sheet to select shapes, not colors. A sketch should only advance if it
still reads clearly when exported at 16px, 32px, and 64px.
